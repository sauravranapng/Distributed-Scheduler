server:
  port: 8080

spring:
  cassandra:
    keyspace-name: tasks
    username: ${ASTRA_USERNAME}
    password: ${ASTRA_PASSWORD}
    schema-action: none

datastax:
  astra:
    secure-connect-bundle: ${SECURE_CONNECT_BUNDLE}
app:
  cassandra:
    schema-script: db/migration/V1__init_schema.cql